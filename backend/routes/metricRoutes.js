// const { serverMetrics } = require("../metrics/metrics");
// const express = require("express");

// const router = express.Router();
// router.get("", async (req, res) => {
//   // res.set('Content-Type', register.contentType);
//   // let metrics = await register.metrics();
//   // res.send(metrics);

//   res.setHeader("Content-type", serverMetrics.register.contentType);
//   res.end(await serverMetrics.register.metrics());
// });
